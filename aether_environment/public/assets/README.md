# Assets Folder

Drop Mixamo characters, Quaternius animation libraries, vehicles, props, textures, and audio here. Files inside `public/` are served statically by Vite, so use paths like `/assets/characters/aether.glb` when loading models.
